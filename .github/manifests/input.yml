# yaml-language-server: $schema=./input.schema.json
description: |
  Copy initial restart to use for the ESM1.6 esm-piControl configuration
  Original file locations are:
  - /g/data/p73/archive/CMIP7/ACCESS-ESM1-6/production/Ndep2-PI-CNP-emissions/restart099/
target: Gadi
type: Release
cold-storage: true
overwrite: false
inputs:
  # Note that the source is an absolute path accessible to the service user
  # Note that the target is a relative path to the given targets inputs directory,
  # eg. Gadi Prerelease is /g/data/vk83/prerelease/configurations/inputs
  /g/data/tm70/sw6175/development/esm1p6/restartfiles/2026.04.19-esm-piControl/: access-esm1p6/modern/pre-industrial-emissions/restart/2026.04.19
